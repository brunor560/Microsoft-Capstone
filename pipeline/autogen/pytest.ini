[pytest]
# Host-side test suite. These tests never call an LLM and never spend tokens.
testpaths = tests
pythonpath = .
addopts = -v --tb=short
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
